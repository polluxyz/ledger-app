<!--
給 Claude Code：草擬 PR 時，**PR 標題與描述都要給，且整份輸出成一個可直接複製的
Markdown 區塊**，依本模板的四節填好，讓開發者一次複製貼上，不必自行從對話中拼湊。

- **標題**：Conventional Commits 格式（`<type>: <簡述>`），與 commit 同慣例用英文。
  squash merge 後它就是 `main` 上的 commit 標題，所以要能獨立看懂。
  標題另外單獨列出，不要混在描述內文裡——GitHub 的標題與內文是兩個輸入框。
- 內容若含程式碼圍欄，外層改用四個反引號以免提早結束。

本註解與下方各節的提示註解一樣，填寫時一併刪除。
-->

## 改了什麼

<!-- 概述這個 PR 的變更內容。一個 PR 聚焦一件事，勿混入無關變更。 -->

## 為什麼

<!-- 動機與背景。若有對應的 spec，連結到 docs/specs/ 的章節。 -->

## 如何測試

<!-- 驗證方式與結果。單行修正等自足小變更可刪除此節，以 CI 結果為準。 -->

- [ ] `pnpm lint`
- [ ] `pnpm typecheck`
- [ ] `pnpm test`

## 影響範圍

<!-- 是否動到資料模型（Prisma schema / migration）或 API 介面？Web / App 端是否受影響？新增環境變數時是否已更新 .env.example？ -->
