# 專案決策脈絡與背景

本文件記錄記帳系統各項技術決策「為什麼這樣選」的理由,作為 `CLAUDE.md`(規範)的補充。CLAUDE.md 說明「要怎麼做」,本文件說明「當初為何這樣決定」,方便延續討論時理解脈絡。

---

## 專案目標(決策的根本依據)

- **主要目的**:個人作品集 / 學習。
- **學習重點**:想熟悉 JavaScript / TypeScript 生態(開發者原本熟 Python)。
- **未來規劃**:專案若完整,可能開源或商業化,故流程與架構需預留擴展空間。

以上目標是所有選型的判斷基準:優先考量「學習價值」與「作品集呈現」,其次才是絕對效能。

---

## 核心技術決策與理由

### 後端:NestJS(而非 Python/FastAPI)

- 開發者熟 Python,但明確想學 JS/TS 生態。
- 全端統一為 TypeScript,型別可在後端/Web/App 間共用,對 monorepo 加分。
- 對記帳這種標準 CRUD,Python 與 Node 在能力/效能上無決定性差距,故以學習價值決定。
- NestJS 架構嚴謹(模組、DI、裝飾器),適合練「可維護/可擴充」的架構思維,作品集顯專業。風格與 FastAPI 相近,概念可互通。

### ORM:Prisma(而非直接寫 SQL)

- 型別安全:從 schema 自動產生型別,查詢結果有完整型別,錯誤在編譯期就抓到(契合全 TS 架構)。
- 防 SQL injection:自動參數化查詢(契合安全要求)。
- Migration:schema 變更可版本控管、可重現(契合企業級流程)。
- 開發效率與可維護性佳。極端複雜查詢可用 `$queryRaw` 降到原生 SQL,保留逃生艙。

### 資料庫:PostgreSQL(而非時序資料庫如 InfluxDB)

- 記帳資料是「帶時間欄位的關聯式資料」,不是「時序資料」。
- 記帳需求:低頻寫入、資料會被修改/刪除、關聯性強(使用者-帳本-交易)、需 ACID 交易完整性(涉及金錢)。這些全是關聯式資料庫的強項,與 TSDB 的甜蜜點(高頻、不可變、純時間軸)相反。
- 時間維度的查詢(月報、區間篩選)PostgreSQL 加索引即可勝任。
- 未來若真有時序需求,可用 TimescaleDB(PostgreSQL 擴充),不需換庫。

### Web 前端:React + Vite(Next.js 為可選)

- 已確定前端框架為 **React**(不變)。
- **Vite 是建構工具,不是框架**;與 React 搭配使用(非二選一)。Vite 作者為 Vue 作者尤雨溪,但 Vite 中立支援 React/Vue/Svelte,React + Vite 為官方標準組合。
- 為何需要建構工具:瀏覽器看不懂 JSX 與 TypeScript,需編譯;專案檔案/套件眾多,需合併最佳化;Vite 提供 HMR 讓開發時即時更新。
- **Next.js 已評估、未採用,勿重新翻案**:其賣點(SSR/SEO)適用於公開內容型網站;本專案為登入後的私人工具,SEO 無用、預渲染幫不上個人化資料,且其伺服器能力有誘使業務邏輯進前端的風險。未來若需公開行銷頁,屆時獨立處理。

### 單一後端原則

- 所有業務邏輯只存在於 NestJS;Web 與 App 為純前端,僅透過 API 取用資料(兩前端須共用同一套 API)。此原則不因前端框架更換而改變,已寫入 CLAUDE.md。

### App:React Native + Expo

- 需同時支援 Android 與 iOS,選跨平台方案。
- React Native 為 TypeScript,可與 Web 端共用語言與型別,學習資產可跨端複用。

### API 風格:RESTful + OpenAPI

- REST(設計風格)契合記帳的資源模型(帳本/交易/分類皆為清楚資源 + 標準 CRUD)。
- OpenAPI(描述規格)由 NestJS 自動產生,兩者搭配,非二選一。
- 動作型操作(如 AI 解析)務實使用動作型端點,如 `POST /ai/parse-transaction`。

---

## AI 記帳功能(未來階段)

- 流程三段:語音→文字(STT)、文字→結構化草稿(LLM 解析)、寫入(呼叫既有交易邏輯)。
- 核心洞察:AI 功能是「把自然語言轉成既有 API 呼叫」的疊加層,不是另一套系統。
- LLM 來源抽象化為 `LLMProvider` 介面,支援雲端(OpenAI/Claude)與本地(Ollama)provider,透過 DI 切換。
- 安全設計:AI 解析結果一律先產生「草稿」供使用者確認/修改,嚴禁未經確認直接寫入(金錢資料容錯率須高)。
- 分階段:先做核心記帳 → 再做 AI 文字版(打字輸入)→ 最後加語音與本地模型。

---

## 開發階段順序重排(2026-08-10)

原規劃「階段二＝AI 文字版」。階段一(後端地基)完成後重新檢視,決定**把 AI 延後、先補前端與社交/借還帳**,新順序為:前端雛形 → 好友+借還帳 → AI → 語音/本地。

**為什麼調整**:

- **AI 是加法層,不是主樑**:AI 功能的本質是「把自然語言轉成既有 API 呼叫」(見上節核心洞察)。它疊在資料模型之上,先做不會讓產品「可用」,反而是把裝潢蓋在還沒完工的房子上。
- **目前沒有任何 UI**:地基雖穩,但整個產品還看不到、點不動。先做前端雛形才能實際驗證 UX、及早發現前後端串接與型別共用的問題,也才有東西可展示(呼應作品集目標)。
- **借還帳會動到核心資料模型**:社交/借還帳(雙邊連動交易 + 債務物件)改變「交易/帳本關係」的本質。若先在舊模型上做 AI,等模型大改就得返工。把模型層的擴張排在 AI 之前,AI 才能蓋在穩定模型上。
- **AI 一定會做,但不急**:開發者明確表示 AI 是遲早要有的功能,可以慢慢來。

**借還帳建模立場(供階段三 spec 延續)**:採「雙邊鏡像連動交易 + 第一級債務物件」——一個借還事件在雙方各自選擇的帳本產生連動交易,並由一個記錄未清餘額、支援多次部分還款與自動結清狀態的債務物件串起;對方須自行確認是否記入及記入哪個帳本(呼應「不未經確認寫入」原則);非使用者的對象先以單邊記錄處理,日後對方加入可升級為同步。細節於階段三專屬 spec 設計。

---

## 帳戶與餘額:取代「付款方式」(2026-08-12 定案)

階段二 2a 才做好的「付款方式」是個沒有語意的標籤。開發者希望改為「**帳戶**」——錢實際放在哪裡、還剩多少。經一輪完整的設計訪談後定案,完整內容見 `docs/specs/phase-2c-accounts.md`,此處只記錄**幾個最關鍵的取捨與理由**。

### 帳戶屬於「使用者」而非「帳本」

決定性的情境:**你用自己的信用卡幫家裡買菜,記在家庭帳本**。若帳戶綁帳本,這張卡得在個人與家庭帳本各建一次,兩邊餘額都是錯的。帳戶綁使用者則跨帳本共用,餘額永遠正確。

這看似違反「帳本是資料隔離的核心邊界」,實則不然:**交易仍屬於帳本、仍依帳本權限隔離**,帳戶只是另一個軸線上的實體,不會讓任何人讀到別人的交易。個人帳戶對其他帳本成員一律隱藏。

### 餘額「算出來」而非「存起來」

儲存餘額欄位,只要有任何一條路徑忘了更新(軟刪除、改金額、改帳戶……)就會**永久失準且難以察覺**。即時計算則不可能失準,因為餘額就是交易的函數。現階段資料量下效能不是問題;日後真的需要,再加快取或物化檢視——**最佳化可以後補,失準的資料補不回來**。

### 帳本可選擇「是否與帳戶連動」

出遊臨時帳本、社團公款這類情境,錢根本不是自己的,不該汙染個人餘額。因此帳本建立時可選擇不連動;非連動帳本的交易不填帳戶、不影響餘額。**建立後不可變更**,否則餘額會突然跳動。

已知且刻意接受的近似:在非連動帳本先墊付的錢不影響餘額,若另把「自己那一份」記進個人帳本,帳面會高於真實現金,**差額恰等於別人欠你的錢**;對方還清後(且不把還款記成收入)帳自動對上。階段三的分攤與債務物件會把這個「影子」變成算得準的數字。

### 封存取代刪除

餘額跨帳本計算後浮現一個漏洞:**別人刪掉共享帳本,你的帳戶餘額會回溯性改變**。因此帳本改為封存;僅當帳本內沒有其他成員的交易時才允許真刪(仍能收拾自己建錯的帳本)。

### 轉帳採新增 `TRANSFER` 型別

一筆交易表達一件事,不會出現「刪了一邊留下孤兒」或「兩邊金額不一致」。改用兩筆連動交易看似不動 schema,卻把風險轉移到「統計時要記得排除成對交易」——忘記排除,收入與支出會同時被灌水且極難察覺。

---

## 階段三的需求素材(2026-08-12 訪談蒐集)

開發者描述的具體場景,供階段三 spec 直接引用:

- **家庭記帳**:共有項目付款、**代墊**、統計每個人的支出、釐清**應該給誰錢**。
- **好友出遊**:這筆帳誰先付、有誰要一起分攤、結束後確認彼此金額。

**重要修正**:分攤 / 代墊 / 借還**不限於共享帳本**。個人帳本同樣需要——「我付了 3000 晚餐,其中 500 是我的,另外 2500 算借給別人」是很常見的個人記帳情境,而且使用者往往根本不會為一頓飯開共享帳本。因此階段三的機制應設計為**與帳本無關**:任何一筆交易都能標記分攤與債權。

另一條線索:**「借出」在結構上就是一種轉帳**——錢從現金離開,但不是支出,而是變成「應收帳款」這項資產。階段三或許可用「特殊帳戶之間的轉帳」表達借還,而非再新增交易型別;待該階段設計時評估。

---

## 離線優先與同步的立場(2026-08-12 討論,未實作)

**構想**:未來 App 版(RN)可離線使用,資料存在手機本機;需要備份或與網頁同步時才登入、上傳雲端。Web 版維持連線模式不變。

**現有設計的三個紅利**(階段一決策的意外收穫,務必保留):

- **UUID 主鍵**:手機離線時就能自行產生 id,不必向伺服器要號。這是離線優先的**前提條件**——若用自增 ID,離線建立的資料會無法合併。
- **軟刪除(`deletedAt`)**:刪除留有「墓碑」可供同步;若是硬刪除,另一台裝置永遠無從得知某筆已被刪除。
- **`updatedAt`**:可支援「取某時間點之後的變更」這種增量同步。

**兩個真正的難點**:

1. **業務邏輯重複**:手機離線時必須自行驗證金額、分類一致性等規則,正面衝撞「業務邏輯只在後端」原則。
   - **緩解方案**:把**純粹的規則**(不碰資料庫者,如「金額須為正整數」「分類型別須與交易一致」)抽到 `packages/shared`(或新設 `packages/domain`),手機與後端**共用同一份 TypeScript 實作**。原則因此由「只在後端」精確化為「**只有一份實作**」——這正是 monorepo 的最大價值。
   - 需要資料庫才能判斷的規則(帳本成員權限、名稱唯一性)仍只能在後端,離線時採樂觀處理、同步時由後端裁決。
2. **衝突解決**:同一筆交易在手機與網頁同時被修改時誰勝出,需設計版本欄位與合併策略。此為獨立設計題,須專屬 spec。

**時機**:待 Web 版定型、且**階段三(好友/借還帳)的資料模型大改完成後**再評估,大致落在階段五前後。在資料模型仍會變動時做同步,等於在流沙上蓋房子。

**現在的影響**:無。後端與 Web 前端都不需為此預作任何改動。唯一要注意的是**別做出讓未來同步變難的決定**(例如改用自增 ID、移除軟刪除)。

---

## Google OAuth 登入(2026-08-12 討論,未實作)

**結論**:確定要做,但**排在 Web MVP 的 Slice 0 完成之後**,以獨立小步進行(規模類似 2a 付款方式)。

**為何不現在做**:

- email / 密碼流程是**地基**,OAuth 只是**額外入口**;先讓地基跑通,風險最低。
- 現在插入會讓 Slice 0 肥大,拖慢「看到可用畫面」這個階段二的核心目標。
- 需要開發者親自到 Google Cloud Console 申請 OAuth 憑證(外部相依),不宜卡在主線上。

**屆時的工作範圍**:

- 後端:`User` 新增 `googleId`(唯一、選填);**`passwordHash` 改為選填**(Google 使用者沒有密碼);新增 `/auth/google` 流程與 ID token 驗證;client id / secret 一律走環境變數。
- 開發者親手:建立 Google Cloud OAuth client、設定 redirect URI。
- 前端:登入頁增加「使用 Google 登入」按鈕。

---

## 開發流程(企業級標準)

- Monorepo(後端 + Web + App 同一 repo,共用型別於 `packages/shared`);workspace 以 **pnpm workspaces** 管理,現階段不引入 Turborepo(避免過度複雜,專案成長後再評估)。
- **階段零(專案初始化)**:repo 建立、scaffolding、CI、分支保護等 GitHub 設定由開發者**親手操作**以達成學習目的,Claude Code 僅從旁說明與產生設定檔內容。
- GitHub Flow:main 保持可部署,feature branch 開發,PR 合併。
- Conventional Commits、PR 流程、main 分支保護、GitHub Actions CI。
- 即使單人開發也完整走流程(練習 + 作品集展現 + 為開源預留)。
- 詳見 `CLAUDE.md`。

---

## 重要提醒

- 選型已定案,勿在延續討論中無故推翻;若情境需要變更,先提出理由與取捨再調整。
- 下一個待展開的工作:**階段二 — 前端雛形(Web)**。詳見 `docs/specs/phase-2-web-mvp.md`。(階段一後端地基已完成:認證、帳本、成員、分類、交易。)
