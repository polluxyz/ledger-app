/**
 * 每位使用者**註冊時**會自動取得的預設帳戶。
 *
 * 注意種子的時機與分類不同：分類屬於帳本，所以在建立帳本時複製；帳戶屬於使用者，
 * 所以在註冊時建立一次，之後所有帳本共用。
 *
 * 與預設分類一樣，這些被視為「使用者資料」（可改名、可刪除），而非固定的系統值。
 * 初始餘額一律為 0，使用者可自行調整成導入當下的真實金額。
 */
export const DEFAULT_ACCOUNTS: readonly string[] = ['現金', '銀行', '信用卡'] as const;
