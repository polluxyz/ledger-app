# ── apps/api ──────────────────────────────────────────────
# PostgreSQL connection string (used by Prisma in a later stage)
DATABASE_URL=

# Secret used to sign JWT tokens (generate a long random string)
JWT_SECRET=

# Port the NestJS API listens on (default: 3000)
PORT=3000